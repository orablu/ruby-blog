<h1>post details</h2>

<div class="postTitle">
    <div class="postLabel">Title:</div>
    <div class="postText"><%= @post.title %></div>
</div>
<div class="postTitle">
    <div class="postLabel">Text:</div>
    <div class="postText"><%= @post.text %></div>
</div>

<h2>comments</h2>
<%= render @post.comments %>

<h2>add a comment</h2>
<%= render 'comments/form' %>

<%= link_to 'edit', edit_post_path(@post) %> | <%= link_to "back", posts_path %>
